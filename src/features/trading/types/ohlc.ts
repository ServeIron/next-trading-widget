/**
 * OHLC (Open, High, Low, Close) Data Types
 */

/**
 * OHLC data structure for a single bar
 */
export interface HoveredBarData {
  open: number;
  high: number;
  low: number;
  close: number;
  time: number;
}

